# ملاحظات التحقق قبل الإصدار

## معاينة الواجهة المبنية للإنتاج

تم في 24 أغسطس 2026 تشغيل خادم Next standalone الناتج من بناء الإنتاج محليًا على المنفذ 3001. أعادت المسارات `/ar/landing` و`/ar/login` و`/ar/register` استجابة HTTP 200.

تمت معاينة `/ar/landing` بصريًا عبر رابط المعاينة. ظهرت صفحة الهبوط العربية باتجاه RTL، وشعار MASAR الأخضر، وروابط تسجيل الدخول وإنشاء الحساب، والباقات الأربع المتطابقة مع الكتالوج الموحد، ووسائل الاتصال المعتمدة: `abadaltwasl390@gmail.com` و`0500909030` وجدة، المملكة العربية السعودية.

> هذه معاينة خادم مؤقت وليست نشرًا دائمًا أو دليلًا على صحة بيانات الإنتاج.

تمت كذلك معاينة صفحة `/ar/login` بصريًا من خادم standalone. ظهر نموذج البريد وكلمة المرور وزر الدخول وروابط التسجيل والدعم والنصوص العربية بتصميم التسويق المنفصل، من دون إرسال بيانات اعتماد أو تنفيذ عملية دخول.

تمت معاينة `/ar/register` بصريًا كذلك. ظهرت خطوات الإعداد الثلاث واختيار النشاط بالعربية وبنمط متوافق مع صفحة الهبوط والدخول. لم يتم اختيار باقة أو إرسال نموذج أو إنشاء حساب خلال التحقق.
