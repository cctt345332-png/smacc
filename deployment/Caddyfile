{
    email {$TLS_EMAIL}
    auto_https prefer_wildcard
}

{$PUBLIC_DOMAIN} {
    encode zstd gzip

    @api path /api/*
    handle @api {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:3000
    }
}
