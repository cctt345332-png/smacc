# حالة الإغلاق قبل الإصدار — MASAR / SMACC ERP

**تاريخ التحقق:** 24 أغسطس 2026

**النطاق:** إغلاق تقني قبل الرفع إلى GitHub أو النشر على خادم خاص.
**الحالة:** **جاهز لمرحلة staging مضبوطة، وليس منشورًا للإنتاج بعد.**

> لم يُنشأ أي commit، ولم يُرفع أي تغيير إلى GitHub، ولم يُنشر النظام على خادم دائم. يبقى القرار بيد مالك المشروع بعد مراجعة هذا الملف.

## ما تم التحقق منه

| البند | النتيجة | الدليل |
|---|---:|---|
| اختبارات إعدادات وأمان الخلفية | ناجح | `pytest -q`: **7 passed** |
| صحة مسار Alembic | ناجح | `python -m alembic heads`: رأس واحد `c5d6e7f8a9b0` |
| صياغة حزم الخلفية | ناجح | `python -m compileall -q app` |
| فحص أنواع الواجهة | ناجح | `npx tsc --noEmit` |
| تدقيق اعتمادات الواجهة الإنتاجية | ناجح | `npm audit --omit=dev --audit-level=high`: **0 vulnerabilities** |
| بناء Next 16 للإنتاج | ناجح | `npm run build -- --webpack` اكتمل، ويولد `.next/standalone/server.js` |
| تشغيل حزمة الواجهة المستقلة | ناجح | خادم standalone بدأ محليًا على المنفذ 3001 |
| صفحات عامة أساسية | ناجح | `/ar/landing` و`/ar/login` و`/ar/register` أعادت HTTP 200 وتمت معاينتها بصريًا |
| فحص التنسيق والأسرار | ناجح | `git diff --check` وفحص مفاتيح/ملفات بيئة متتبعة لم يعثرا على نتائج عالية الثقة |
| أدوات النسخ الاحتياطي والاستعادة | ناجح محليًا | تحقق الصياغة `sh -n` ناجح؛ لم تُنفذ ضد قاعدة بيانات حقيقية |

تفاصيل المعاينة المرئية محفوظة في [`deployment/verification-notes.md`](deployment/verification-notes.md).

## التغييرات التي تؤهل النشر

| المجال | الإجراء المنجز |
|---|---|
| إعدادات الخلفية | دعم الأسماء المختصرة للبيئة مثل `PROD` مع تحويلها إلى `production`، مع بقاء رفض الأسرار وقاعدة البيانات وCORS غير الآمنة في staging والإنتاج. |
| CORS | أصبحت الأصول الإنتاجية تقرأ من `CORS_ORIGINS` صراحةً؛ لا تُضاف localhost أو نطاقات المعاينة إلا في development وpreview. |
| الترحيلات | استبدال `create_all()` عند تشغيل الحاوية بـ `python -m alembic upgrade head`. |
| الواجهة | ترقية الحزم الأمنية ثم ترحيل واجهات المسارات غير المتزامنة واصطلاح Proxy في Next 16؛ البناء والأنواع ناجحان. |
| التشغيل | إضافة `docker-compose.production.yml` و`deployment/Caddyfile`: HTTPS عبر Caddy، PostgreSQL وRedis على شبكة داخلية فقط، ولا أسرار افتراضية. |
| البيئة | إضافة `.env.production.example` و`.env.development.example`؛ كلاهما نموذج فقط ولا يحتويان بيانات تشغيل خاصة. |
| النسخ الاحتياطي | إضافة `deployment/backup-postgres.sh` و`deployment/restore-postgres.sh` مع checksum، مدة احتفاظ قابلة للضبط، وتأكيد صريح قبل الاستعادة. |
| الجودة المستمرة | إضافة `.github/workflows/release-gates.yml` لفحص الأنواع والتدقيق الأمني وبناء الواجهة واختبارات الخلفية عند الرفع أو طلب الدمج. |

## ما لم يُعتمد بعد

| بند متبقٍ | السبب | الإجراء المطلوب |
|---|---|---|
| اختبار Compose والإقلاع الكامل | Docker غير متاح في بيئة المعاينة الحالية. | نفّذ اختبار `docker compose ... config` ثم build وhealth checks على خادم staging أو الخادم الخاص قبل فتح الإنتاج. |
| ترحيلات قاعدة بيانات حقيقية | تم فحص رأس Alembic فقط، ولم تُشغّل الترحيلات على نسخة staging من بياناتكم. | خذ نسخة احتياطية، ثم نفّذ الترحيلات على staging واختبر التسجيل والفواتير والمندوبين والتقارير. |
| فحص تدفقات الأعمال الكاملة | تحققنا من البناء وصفحات البداية، وليس من كل عملية محاسبية ببيانات الإنتاج. | نفّذ UAT موجزًا للحسابات والصلاحيات والفواتير والمرتجعات والطباعة والتقارير والمندوبين. |
| مراجعة مجموعة التغييرات | الشجرة ما زالت كبيرة: **228 مسارًا متغيرًا/غير متتبع** عند آخر جرد، وHEAD في وضع detached عند `6291e0f`. | راجع التغييرات ونظمها في commits منطقية فقط بعد تفويض صريح. |
| النسخ خارج الخادم | النص البرمجي يحفظ على المضيف فقط. | اربط `BACKUP_DIR` بتخزين منفصل خارج الخادم واختبر الاستعادة عليه. |

## تعليمات النشر على الخادم بعد الاعتماد

يتطلب هذا المسار نطاقًا يوجه إلى عنوان الخادم، وخادمًا عليه Docker وDocker Compose، ومنافذ 80 و443 مفتوحة. لا تضع أي قيمة حقيقية في Git.

```bash
# على الخادم، داخل مجلد المشروع بعد وصول الكود المعتمد إليه
cp .env.production.example .env.production
chmod 600 .env.production
# املأ .env.production بقيم عشوائية قوية وبالنطاق الحقيقي.

# تحقق من الإعداد قبل التشغيل
sudo docker compose --env-file .env.production -f docker-compose.production.yml config

# ابنِ وشغّل الخدمات
sudo docker compose --env-file .env.production -f docker-compose.production.yml up -d --build
sudo docker compose --env-file .env.production -f docker-compose.production.yml ps
sudo docker compose --env-file .env.production -f docker-compose.production.yml logs --tail=150 backend caddy
```

بعد نجاح Caddy في إصدار الشهادة، افحص `https://<PUBLIC_DOMAIN>/ar/landing` و`https://<PUBLIC_DOMAIN>/api/v1/health`. لا تفعّل المستخدم العام أو تغير DNS النهائي قبل اجتياز اختبار staging ووجود نسخة احتياطية قابلة للاستعادة.

## ما أحتاجه للخطوة التالية

| المسار | المعلومات أو الموافقة المطلوبة |
|---|---|
| GitHub | موافقة صريحة على إنشاء commits منطقية ورفعها، مع تأكيد الفرع الهدف في المستودع `cctt345332-png/smacc`. |
| الخادم | عنوان SSH أو طريقة الوصول، اسم المستخدم، مسار النشر، والنطاق النهائي. لا ترسل كلمة المرور هنا؛ استخدم طريقة وصول آمنة. |
| DNS وTLS | تأكيد أن سجل A/AAAA للنطاق سيشير إلى الخادم، وبريد تشغيلي لإصدار شهادة TLS. |
| الأسرار | تُنشأ وتوضع مباشرة في `.env.production` على الخادم؛ لا تُرسل في المحادثة أو المستودع. |
| النسخ الاحتياطي | مسار تخزين خارج الخادم، ومدة الاحتفاظ المطلوبة، وخطة اختبار استعادة أولى. |

## قرار الإصدار

**القرار التقني الحالي:** يمكن الانتقال إلى staging بعد الحصول على بيانات الخادم اللازمة. لا يجوز اعتبار المشروع منشورًا أو جاهزًا تجاريًا نهائيًا قبل اختبار Compose والترحيلات وUAT على بيئة تشبه الإنتاج، ثم اعتماد مستقل للرفع والنشر.
